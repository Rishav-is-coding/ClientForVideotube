import React from 'react';
import ChannelProfile from '../components/ChannelProfile';

export default function Channel() {
  return (
    <div>
      <ChannelProfile />
      {/* Add channel videos, playlists, etc. */}
    </div>
  );
}
